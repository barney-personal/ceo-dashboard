#!/bin/sh

branch="$(git rev-parse --abbrev-ref HEAD 2>/dev/null)"

if [ "$branch" = "main" ] || [ "$branch" = "master" ]; then
  echo "Committing on $branch is blocked. Create or switch to a feature branch first."
  exit 1
fi

exit 0
